[package]
edition = "2021"
name = "state"
version = "0.1.0"

[[bin]]
name = "state"
path = "main.rs"

[dependencies]
cursive = {version = "0.19", default-features = false, features = ["termion-backend"]}
